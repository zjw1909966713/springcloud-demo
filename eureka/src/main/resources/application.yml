eureka:
  client:
    register-with-eureka: false  #禁止自己注册
    fetch-registry: false
    service-url:
      defaultZone:  http://localhost:8761/eureka/
  server:
    enable-self-preservation: true #是否自动保留，自我保护 （真实环境不要关闭）
spring:
  application:
    name: eureka